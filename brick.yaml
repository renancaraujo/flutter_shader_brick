name: flutter_shader
description: Template for generating GLSL files to be used within a Flutter project.
version: 0.1.0

environment:
  mason: ">=0.1.0-dev.50 <0.1.0"

vars:
  name:
    type: string
    description: The shader name
    prompt: What is the shader file name?
